
//Store unique elements in an undefined order
// if we create an empty set , we must specify the type
// A set can a null element, but only one
fun main() {

    var number = setOf(4,7,null,9,2)
    print(number)
}